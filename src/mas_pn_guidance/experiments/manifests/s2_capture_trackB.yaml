# Ticket 024 S3 track B — INSTRUMENTED RE-RECORD capture (rev1 §8/§11.1).
# Minimal capture of the matched-smoother divergence cell (SM vf5, tau=0) through the
# now-instrumented rosbag_record_reduced.sh, so the bag carries the peer ray topics
# (target_rays_w_raw / target_rays_w) + coop_loc/solver_diagnostics that the earlier
# reduced bags omitted. Run sim_fusion_matched_fgo.tmuxp (mock GT peer arm).
# Separate results_dir so the S2 evidence is NOT overwritten (CLAUDE.md).
session:
  interceptor_ns: px4_1
  observer_ns: px4_2
  target_ns: px4_3
  interceptor_ic: [0.0, -50.0]
  ic_tol_m: 1.5
  seed: 42
  use_sim_time: true
  record: true
  bag_script: /home/usrg/mas/bag/rosbag_record_reduced.sh
  bag_dir: /home/usrg/mas/bag
output:
  results_dir: /home/usrg/mas/src/mas_pn_guidance/experiments/results/s3_trackB_capture_20260717
gate:
  geometry: crossing
  forward_speed: 4.0
  lateral_accel: 0.0
  estimator: oracle
  max_cpa_m: 0.5
policies:
  settle_error_streak_abort: 3
  on_boot_abort: continue
boots:
  - id: CAPSM_T0
    geometries: crossing
    forward_speeds: [5]
    lateral_accels: [0]
    estimators: [cooperative]
    repeats: 3
    live_params: {/px4_1/ray_delay_matched: {latency_s: 0.0}}
